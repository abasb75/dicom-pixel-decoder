import { DecodeOptions } from "../types";
import { PixelArray } from "../types";
declare function yrbFullToRgba(pixelData: PixelArray, pixelModule: DecodeOptions): PixelArray;
export default yrbFullToRgba;
